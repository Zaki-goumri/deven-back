### 1. Redundancy in `mail.config.ts`

There's a comment `//Do not touch this config until I fix the redundancy issue`. It seems there's a duplicate transport configuration. This should be cleaned up to avoid confusion.